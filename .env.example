# Postgres (used by docker-compose and the API)
POSTGRES_USER=panopticon
POSTGRES_PASSWORD=panopticon
POSTGRES_DB=panopticon
POSTGRES_PORT=5432

# Derived — copy this into api/.env as DATABASE_URL
DATABASE_URL=postgres://panopticon:panopticon@localhost:5432/panopticon
